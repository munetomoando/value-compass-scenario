# 価値観コンパス v0.3「2つの人生で発見する」実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.2 の単一文脈16問DCEを、2ライフ文脈（独身／既婚・子1人）×各10問のDCEに拡張し、文脈間の選好シフト（ΔWTP）を結果として返す新アプリ `value-compass-scenario` を、学生展開中の v0.2 に一切触れず別リポジトリ・別URL・別KVで構築する。

**Architecture:** v0.2 のクライアント完結アーキテクチャ（GitHub Pages フロント＋Cloudflare Worker＋KV、推定はブラウザ内の純関数）を踏襲。推定は「全20問を1つの罰則付きロジットにプールし、`属性×シナリオ` 交互作用でシフトを推定」。MRS（=β_attr/β_income）はスケール不変なため、シナリオ別MRSとΔWTPはスケール差に頑健。識別がタイト（20観測×最大12パラメータ）なため交互作用に強いリッジ＋4属性フォールバックで安定化。

**Tech Stack:** バニラJS（UMD/IIFE）、Node.js `node:test`（テスト）、Node ESM スクリプト（設問生成・KVエクスポート）、Cloudflare Workers + Wrangler 4 + KV、GitHub Pages（gh-pages サブツリー）。

**設計書:** `20260604価値観コンパスv03設計書.html`（本計画の上位仕様）

---

## ファイル構成（新リポジトリ `value-compass-scenario` 内）

| パス | 役割 | 変更種別 |
|------|------|----------|
| `value-compass/frontend/questions.json` | 2ブロック×10問＋練習＋支配の設問データ＋シナリオメタ | 刷新 |
| `value-compass/frontend/build-questions.mjs` | 設問生成（ブロック別バランス最適化） | 改修 |
| `value-compass/frontend/estimate.js` | プールロジット＋交互作用＋ΔWTP＋スケール診断＋フォールバック | 刷新 |
| `value-compass/frontend/radar.js` | 2シナリオ重ね描きSVG | 改修 |
| `value-compass/frontend/app.js` | 2ブロック進行・シナリオ画面・順序ランダム化・payload v0.3 | 改修 |
| `value-compass/frontend/index.html` | シナリオ提示/切替画面・結果6カード目のDOM | 改修 |
| `value-compass/frontend/style.css` | シナリオ画面・重ねレーダー・ΔWTPカード | 改修 |
| `value-compass/frontend/test/*.test.mjs` | 各純関数のテスト | 改修・追加 |
| `value-compass/worker/src/index.js` | 受信ログに v0.3 フィールドを保存 | 小改修 |
| `value-compass/worker/wrangler.toml` | name・KV id・ALLOWED_ORIGIN | 改修 |
| `export-from-kv.mjs` | CSV出力列に v0.3 フィールド追加 | 改修 |
| `deploy-frontend.sh` / `deploy-worker.sh` / `enable-pages.sh` | REPO 名の更新 | 小改修 |

**テスト実行コマンド（共通）:** リポジトリ直下から
```bash
( cd value-compass/frontend && node --test )
```
期待：全 `test/*.test.mjs` が PASS。

---

## Task 0: 新リポジトリ・インフラのスキャフォールド

学生展開中の v0.2 と完全分離するため、現行コードを複製した新リポジトリと新インフラを用意する。これはコード変更前の土台づくり。

**Files:**
- Create（ローカル）: 兄弟ディレクトリ `../compass-scenario/`（現行 `compass/` の複製）
- Modify: `value-compass/worker/wrangler.toml`
- Modify: `value-compass/frontend/app.js:10`
- Modify: `enable-pages.sh`

- [ ] **Step 1: 現行コードを新ディレクトリへ複製（gitメタは持ち込まない）**

リポジトリ直下（`compass/`）の親で実行：
```bash
cd /Users/munetomoando/claude-work/nuce
rsync -a --exclude='.git' --exclude='.wrangler' --exclude='node_modules' \
  --exclude='*.csv' --exclude='.playwright-mcp' compass/ compass-scenario/
cd compass-scenario && git init -b main
```
期待：`compass-scenario/` に v0.2 のソースがコピーされ、新規 git リポジトリが初期化される。

- [ ] **Step 2: GitHub に新リポジトリを作成して push（ユーザー操作が必要な場合あり）**

```bash
cd /Users/munetomoando/claude-work/nuce/compass-scenario
git add -A && git commit -m "chore: seed value-compass-scenario from v0.2"
gh repo create munetomoando/value-compass-scenario --private --source=. --remote=origin --push
```
期待：`https://github.com/munetomoando/value-compass-scenario` が作成され main が push される。
（`gh auth` 未認証なら、ユーザーに `! gh auth login` を依頼する。）

- [ ] **Step 3: 新KV名前空間を作成**

```bash
cd /Users/munetomoando/claude-work/nuce/compass-scenario/value-compass/worker
wrangler kv namespace create LOGS
wrangler kv namespace create LOGS --preview
```
期待：本番・preview それぞれの `id` が出力される。両方を控える。

- [ ] **Step 4: wrangler.toml を新アプリ用に書き換え**

`value-compass/worker/wrangler.toml` を次のように修正：
- `name = "value-compass"` → `name = "value-compass-scenario"`
- `[[kv_namespaces]]` の `id` / `preview_id` を Step 3 の新IDに置換
- `ALLOWED_ORIGIN = "https://munetomoando.github.io"` はそのまま（同一Pagesオリジン）

- [ ] **Step 5: フロントの送信先を新Workerに変更**

`value-compass/frontend/app.js:10` を修正：
```javascript
  const SUBMIT_ENDPOINT = "https://value-compass-scenario.ando-munetomo.workers.dev/api/submit";
```

- [ ] **Step 6: デプロイスクリプトのREPO名を更新**

`enable-pages.sh` の `REPO=munetomoando/value-compass` を `REPO=munetomoando/value-compass-scenario` に変更。`deploy-frontend.sh` / `deploy-worker.sh` はパス相対のため変更不要（確認のみ）。

- [ ] **Step 7: コミット**

```bash
cd /Users/munetomoando/claude-work/nuce/compass-scenario
git add -A && git commit -m "chore: point infra at value-compass-scenario (worker/KV/endpoint/pages)"
```

> 以降のすべての Task は `compass-scenario/` 内で作業する。

---

## Task 1: 設問生成を2ブロック×10問へ刷新

`build-questions.mjs` を、シナリオA/B各10問＋練習＋支配を生成するよう改修し、各ブロック単独でのバランス（各属性が十分分岐・income4水準が出現）を満たす。

**Files:**
- Modify: `value-compass/frontend/build-questions.mjs`
- Modify: `value-compass/frontend/questions.json`（生成物）
- Test: `value-compass/frontend/test/questions.test.mjs`

- [ ] **Step 1: questions.test.mjs を v0.3 構成に書き換え（失敗するテスト）**

`test/questions.test.mjs` 全体を次に置換：
```javascript
import { test } from "node:test";
import assert from "node:assert";
import { readFileSync } from "node:fs";

const data = JSON.parse(readFileSync(new URL("../questions.json", import.meta.url)));
const { meta, questions } = data;
const mainsA = questions.filter(q => q.scored && q.scenario === "A");
const mainsB = questions.filter(q => q.scored && q.scenario === "B");

test("各シナリオ10問・練習1・支配1の構成", () => {
  assert.equal(mainsA.length, 10);
  assert.equal(mainsB.length, 10);
  assert.equal(questions.filter(q => q.type === "practice").length, 1);
  assert.equal(questions.filter(q => q.type === "dominant").length, 1);
});

test("メタにシナリオ定義がある", () => {
  assert.deepEqual(meta.framing.scenarios, ["single_no_plan", "married_one_child"]);
});

test("各ブロック単独で全6属性が十分に分岐する（5〜9回）", () => {
  for (const mains of [mainsA, mainsB]) {
    for (const a of meta.attribute_order) {
      const n = mains.filter(q => q.A[a] !== q.B[a]).length;
      assert.ok(n >= 5 && n <= 9, `${a} の分岐 ${n} が範囲外`);
    }
  }
});

test("各ブロックで income 4水準すべてが出現する", () => {
  for (const mains of [mainsA, mainsB]) {
    const seen = new Set();
    for (const q of mains) { seen.add(q.A.income); seen.add(q.B.income); }
    assert.deepEqual([...seen].sort(), ["300", "450", "600", "750"]);
  }
});

test("本番に支配ペア（トレードオフ無し）が無い", () => {
  const GOOD = { location: "なし", hours: "少", remote: "可", growth: "大", stability: "安定" };
  const better = (attr, a, b) => attr === "income"
    ? (+a.income >= +b.income ? "A" : "B")
    : (a[attr] === GOOD[attr] ? "A" : (b[attr] === GOOD[attr] ? "B" : null));
  for (const q of [...mainsA, ...mainsB]) {
    const ds = meta.attribute_order.filter(k => q.A[k] !== q.B[k]);
    const sides = ds.map(k => better(k, q.A, q.B));
    assert.ok(!(sides.every(s => s === "A") || sides.every(s => s === "B")), `${q.id} が支配的`);
  }
});

test("支配選択qdはAが全属性で良い・採点対象外", () => {
  const q = questions.find(q => q.type === "dominant");
  const GOOD = { location: "なし", hours: "少", remote: "可", growth: "大", stability: "安定" };
  assert.ok(+q.A.income > +q.B.income);
  for (const a of ["location", "hours", "remote", "growth", "stability"]) assert.equal(q.A[a], GOOD[a]);
  assert.equal(q.scored, false);
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
( cd value-compass/frontend && node --test test/questions.test.mjs )
```
期待：FAIL（現行 questions.json は16問・scenario未設定のため）。

- [ ] **Step 3: build-questions.mjs を2ブロック生成に改修**

`META` の `framing` にシナリオ定義を追加（`build-questions.mjs:16-20` の `framing` ブロックを置換）：
```javascript
  framing: {
    income_anchor: "age30_gross",
    scenarios: ["single_no_plan", "married_one_child"],
    note: "年収はその仕事に就いた30歳頃の額面。転勤・在宅などはその仕事がずっと持つ特徴。各シナリオの家族設定でどう感じるかを率直に選んでください。"
  },
```

`penalty` 関数を10問用に置換（`build-questions.mjs:71-84`）：
```javascript
function penalty(mains){
  let pen=0;
  for(const k of BIN_KEYS){
    const n = mains.filter(q=>q.A[k]!==q.B[k]).length;
    const target=7; pen += Math.abs(n-target); // 10問中 ~7問で各2値属性が分岐するのが目標
  }
  const incBranch = mains.filter(q=>q.A.income!==q.B.income).length;
  pen += Math.abs(incBranch-8); // 年収は分岐しやすいため目標を ~8 に設定
  const cnt = Object.fromEntries(INCOME.map(v=>[v,0]));
  for(const q of mains){ cnt[q.A.income]++; cnt[q.B.income]++; }
  const avg = (10*2)/4;
  pen += INCOME.reduce((s,v)=>s+Math.abs(cnt[v]-avg),0)*0.5;
  return pen;
}
```

`buildMains` を「任意の問数・シード」で生成できるよう一般化（`build-questions.mjs:86-97` を置換）：
```javascript
function buildMains(seed, count){
  const r=rng(seed);
  let best=null, bestPen=Infinity;
  for(let attempt=0; attempt<6000; attempt++){
    const mains=[];
    for(let i=0;i<count;i++){ const p=makePair(r); if(p) mains.push(p); }
    if(mains.length<count) continue;
    const pen=penalty(mains);
    if(pen<bestPen){ bestPen=pen; best=mains; if(pen<=5) break; }
  }
  return { mains:best, pen:bestPen };
}
```

`build` 関数を2ブロック出力に置換（`build-questions.mjs:99-115`）：
```javascript
function build(){
  const blockA = buildMains(20260604, 10);
  const blockB = buildMains(20260605, 10);
  if(!blockA.mains || !blockB.mains) throw new Error("設問生成に失敗");
  const questions = [];
  questions.push({ id:"practice", type:"practice", scored:false, scenario:null, intent:"操作に慣れる",
    A:{income:"450",location:"なし",hours:"少",remote:"可",growth:"小",stability:"安定"},
    B:{income:"450",location:"あり",hours:"多",remote:"不可",growth:"大",stability:"不安定"} });
  blockA.mains.forEach((m,i)=> questions.push({ id:"a"+String(i+1).padStart(2,"0"), type:"main", scored:true,
    scenario:"A", intent:"トレードオフ", A:m.A, B:m.B }));
  blockB.mains.forEach((m,i)=> questions.push({ id:"b"+String(i+1).padStart(2,"0"), type:"main", scored:true,
    scenario:"B", intent:"トレードオフ", A:m.A, B:m.B }));
  questions.push({ id:"qd", type:"dominant", scored:false, scenario:null, intent:"注意チェック（Aが全優位）",
    A:{income:"750",location:"なし",hours:"少",remote:"可",growth:"大",stability:"安定"},
    B:{income:"300",location:"あり",hours:"多",remote:"不可",growth:"小",stability:"不安定"} });

  writeFileSync(new URL("./questions.json",import.meta.url),
    JSON.stringify({ meta:META, questions }, null, 2)+"\n");
  console.log("生成完了 penaltyA=",blockA.pen," penaltyB=",blockB.pen);
}
build();
```

また `META.app_version` を `"0.3"` に変更（`build-questions.mjs:15`）。

- [ ] **Step 4: 生成を実行**

```bash
( cd value-compass/frontend && node build-questions.mjs )
```
期待：`生成完了 penaltyA= … penaltyB= …` が表示され `questions.json` が更新される。penalty が大きい（>8）場合はシードを変えて再生成。

- [ ] **Step 5: テストを実行して PASS を確認**

```bash
( cd value-compass/frontend && node --test test/questions.test.mjs )
```
期待：PASS。失敗する属性があればシード（20260604/20260605）を増分して Step 4-5 を繰り返す。

- [ ] **Step 6: コミット**

```bash
git add value-compass/frontend/build-questions.mjs value-compass/frontend/questions.json value-compass/frontend/test/questions.test.mjs
git commit -m "feat(questions): 2シナリオ×各10問の設問生成とブロック別バランス検証"
```

---

## Task 2: 推定エンジンをプールロジット＋ΔWTPに刷新

`estimate.js` に、全20問をプールして `属性×シナリオ` 交互作用を推定する `estimate2` を追加し、シナリオ別MRSとΔWTPを返す。`extras`（決定的瞬間・品質）はブロック横断で動くよう小改修。

**Files:**
- Modify: `value-compass/frontend/estimate.js`
- Test: `value-compass/frontend/test/estimate.test.mjs`（刷新）

- [ ] **Step 1: estimate.test.mjs を v0.3 仕様に書き換え（失敗するテスト）**

`test/estimate.test.mjs` 全体を次に置換：
```javascript
import { test } from "node:test";
import assert from "node:assert";
import { readFileSync } from "node:fs";
import E from "../estimate.js";

const data = JSON.parse(readFileSync(new URL("../questions.json", import.meta.url)));
const { meta, questions } = data;

// シナリオA: 年収重視で在宅は手放す。シナリオB: 在宅・残業少を強く守る。
function choiceA(q){ // income優先
  if (+q.A.income !== +q.B.income) return +q.A.income > +q.B.income ? "A" : "B";
  return "A";
}
function choiceB(q){ // remote→hours→location を守る
  for (const k of ["remote","hours","location"]) {
    const good = {remote:"可",hours:"少",location:"なし"}[k];
    if (q.A[k] === good && q.B[k] !== good) return "A";
    if (q.B[k] === good && q.A[k] !== good) return "B";
  }
  return +q.A.income > +q.B.income ? "A" : "B";
}
function synth(){
  return questions.filter(q => q.scored).map(q => ({
    q_id: q.id,
    choice: q.scenario === "A" ? choiceA(q) : choiceB(q),
    response_ms: 5000,
    scenario: q.scenario
  }));
}

test("estimate2 は beta_A/beta_B/gamma/mrs/delta を返す", () => {
  const r = E.estimate2(questions, meta, synth());
  for (const a of meta.attribute_order) {
    assert.ok(Number.isFinite(r.beta_A[a]), `beta_A.${a}`);
    assert.ok(Number.isFinite(r.beta_B[a]), `beta_B.${a}`);
    assert.ok(Number.isFinite(r.gamma[a]), `gamma.${a}`);
  }
  assert.ok("mrs_A_manyen" in r && "mrs_B_manyen" in r && "delta_wtp_manyen" in r);
  assert.ok(typeof r.scale_diff === "number");
  assert.equal(typeof r.fallback_constrained, "boolean");
});

test("育児シナリオで在宅の価値が上がる（gamma.remote>0, delta.remote>0）", () => {
  const r = E.estimate2(questions, meta, synth());
  assert.ok(r.gamma.remote > 0, `gamma.remote=${r.gamma.remote}`);
  if (r.mrs_A_manyen.remote !== null && r.mrs_B_manyen.remote !== null) {
    assert.ok(r.delta_wtp_manyen.remote > 0, `delta.remote=${r.delta_wtp_manyen.remote}`);
  }
});

test("MRSは ±500 でクリップされ 5万円刻み", () => {
  const r = E.estimate2(questions, meta, synth());
  for (const m of [r.mrs_A_manyen, r.mrs_B_manyen]) {
    for (const a of Object.keys(m)) {
      if (m[a] === null) continue;
      assert.ok(m[a] >= -500 && m[a] <= 500);
      assert.equal(m[a] % 5, 0);
    }
  }
});

test("交互作用を4属性に限定したフォールバックでは gamma.income/growth が0", () => {
  const r = E.estimate2(questions, meta, synth(), { forceConstrained: true });
  assert.equal(r.gamma.income, 0);
  assert.equal(r.gamma.growth, 0);
  assert.equal(r.fallback_constrained, true);
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
( cd value-compass/frontend && node --test test/estimate.test.mjs )
```
期待：FAIL（`E.estimate2` が未定義）。

- [ ] **Step 3: estimate.js に重み付き罰則ロジットと estimate2 を実装**

`estimate.js` の `fitLogit` 直後（`estimate.js:54` の後）に、列ごとに罰則を変えられる版を追加：
```javascript
  // 列ごとに異なるリッジ罰則をかける二項ロジット（penalties: 長さpの配列）
  function fitLogitW(X, y, penalties, opts){
    if (!X || X.length === 0) throw new Error("fitLogitW: X must be non-empty");
    opts = opts || {};
    const maxIter = opts.maxIter || 60;
    const p = X[0].length;
    let beta = new Array(p).fill(0);
    let converged = false;
    for(let it=0; it<maxIter; it++){
      const g = new Array(p).fill(0);
      const H = Array.from({length:p},()=>new Array(p).fill(0));
      for(let i=0;i<X.length;i++){
        let eta=0; for(let j=0;j<p;j++) eta+=X[i][j]*beta[j];
        const mu=1/(1+Math.exp(-eta)); const w=Math.max(mu*(1-mu),1e-6);
        for(let j=0;j<p;j++){
          g[j]+=X[i][j]*(y[i]-mu);
          for(let k=0;k<p;k++) H[j][k]+=X[i][j]*X[i][k]*w;
        }
      }
      for(let j=0;j<p;j++){ const pen=penalties[j]||0; g[j]-=pen*beta[j]; H[j][j]+=pen; }
      const step = solve(H, g);
      let maxd=0; for(let j=0;j<p;j++){ beta[j]+=step[j]; maxd=Math.max(maxd,Math.abs(step[j])); }
      if(maxd<1e-7){ converged=true; break; }
    }
    return { beta, converged };
  }
```

同ファイルの `estimate` 関数の直後（`estimate.js:110` の後）に、MRS算出ヘルパと `estimate2` を追加：
```javascript
  // βマップ→MRS（万円）マップ。bInc=β_income。v0.2 §6.3 のクリップ/null処理を踏襲
  function mrsFromBeta(order, betaMap, bInc){
    const out={};
    for(const a of order){
      if(a==="income") continue;
      if(!Number.isFinite(bInc) || bInc<0.02){ out[a]=null; continue; }
      let v=(betaMap[a]/bInc)*100;
      if(!Number.isFinite(v)){ out[a]=null; continue; }
      out[a]=Math.max(-500,Math.min(500, Math.round(v/5)*5));
    }
    return out;
  }

  // プール推定：全20問を1モデルに入れ、属性×シナリオ交互作用でシフトを推定
  //   answers: [{ q_id, choice, response_ms, scenario:"A"|"B" }]
  //   opts.forceConstrained=true で交互作用を hours/remote/location/stability に限定
  function estimate2(questions, meta, answers, opts){
    opts = opts || {};
    const order = meta.attribute_order;          // 6属性
    const SHIFT = ["location","hours","remote","stability"]; // 限定時に動かす属性
    const ansById={}; answers.forEach(a=>ansById[a.q_id]=a);
    const mains = questions.filter(q=>q.scored);

    function fit(constrained){
      // 列: [intercept, 6 main, 交互作用(6 or 4)]
      const intCols = constrained ? SHIFT : order;
      const X=[], y=[];
      for(const q of mains){
        const a=ansById[q.id]; if(!a) continue;
        const d=designRow(meta,q);                 // 長さ6
        const s = (a.scenario||q.scenario)==="B" ? 1 : 0;
        const inter = intCols.map((attr)=> s * d[order.indexOf(attr)]);
        X.push([1, ...d, ...inter]); y.push(a.choice==="A"?1:0);
      }
      // 罰則：intercept=0, main=0.5, 交互作用=1.5（強め）
      const pen=[0, ...order.map(()=>0.5), ...intCols.map(()=>1.5)];
      const { beta:bf, converged } = fitLogitW(X, y, pen);
      const beta_A={}, gamma={};
      order.forEach((a,i)=> beta_A[a]=bf[i+1]);
      order.forEach(a=> gamma[a]=0);
      intCols.forEach((attr,j)=> gamma[attr]=bf[1+order.length+j]);
      return { beta_A, gamma, converged };
    }

    let constrained = !!opts.forceConstrained;
    let f = fit(constrained);
    // 非収束 or γが極端（|γ|>8）なら4属性フォールバック
    const blew = !f.converged || order.some(a=>Math.abs(f.gamma[a])>8);
    if(blew && !constrained){ constrained=true; f=fit(true); }

    const beta_A=f.beta_A, gamma=f.gamma;
    const beta_B={}; order.forEach(a=> beta_B[a]=beta_A[a]+gamma[a]);

    const mrs_A=mrsFromBeta(order, beta_A, beta_A.income);
    const mrs_B=mrsFromBeta(order, beta_B, beta_B.income);
    const delta={};
    for(const a of order){ if(a==="income") continue;
      delta[a]=(mrs_A[a]===null||mrs_B[a]===null)?null:(mrs_B[a]-mrs_A[a]); }

    // 重要度（レーダー用、各シナリオ）= |β|*sd(差分列)、最大1正規化
    function importanceOf(betaMap, flt){
      const cols=order.map(a=>[]);
      mains.forEach(q=>{ const a=ansById[q.id]; if(!a) return;
        if((a.scenario||q.scenario)!==flt) return;
        const d=designRow(meta,q); order.forEach((_,i)=>cols[i].push(d[i])); });
      const imp={}; order.forEach((a,i)=> imp[a]=Math.abs(betaMap[a])*std(cols[i]));
      const mx=Math.max(...Object.values(imp),1e-9); const out={};
      order.forEach(a=> out[a]=imp[a]/mx); return out;
    }
    const importance_A=importanceOf(beta_A,"A"), importance_B=importanceOf(beta_B,"B");
    const rankA=order.slice().sort((x,z)=>importance_A[z]-importance_A[x]);
    const rankB=order.slice().sort((x,z)=>importance_B[z]-importance_B[x]);

    // スケール診断（不変MRSと別に保持）：β係数ベクトルのノルム比の対数
    const norm=m=>Math.sqrt(order.reduce((s,a)=>s+m[a]*m[a],0));
    const scale_diff = +(Math.log((norm(beta_B)+1e-9)/(norm(beta_A)+1e-9))).toFixed(3);

    return { beta_A, beta_B, gamma,
             mrs_A_manyen:mrs_A, mrs_B_manyen:mrs_B, delta_wtp_manyen:delta,
             importance_A, importance_B, importance_rank_A:rankA, importance_rank_B:rankB,
             scale_diff, fallback_constrained:constrained, converged:f.converged };
  }
```

最後に export 行（`estimate.js:171`）へ `estimate2`, `fitLogitW`, `mrsFromBeta` を追加：
```javascript
  return { fitLogit, fitLogitW, solve, estimate, estimate2, mrsFromBeta, extras, designRow, code };
```

- [ ] **Step 4: テストを実行して PASS を確認**

```bash
( cd value-compass/frontend && node --test test/estimate.test.mjs )
```
期待：PASS（4テスト）。`gamma.remote>0` が出ない場合は合成回答の意図と designRow の符号を照合してデバッグ。

- [ ] **Step 5: 既存の logit.test.mjs が壊れていないか確認**

```bash
( cd value-compass/frontend && node --test test/logit.test.mjs )
```
期待：PASS（`fitLogit` は変更していないため）。

- [ ] **Step 6: コミット**

```bash
git add value-compass/frontend/estimate.js value-compass/frontend/test/estimate.test.mjs
git commit -m "feat(estimate): プールロジット＋シナリオ交互作用＋ΔWTP＋4属性フォールバック"
```

---

## Task 3: レーダーチャートの2シナリオ重ね描き

`radar.js` に、2つの重要度プロファイルを重ねて描く `buildRadarOverlaySVG` を追加する。

**Files:**
- Modify: `value-compass/frontend/radar.js`
- Test: `value-compass/frontend/test/radar.test.mjs`

- [ ] **Step 1: radar.test.mjs に重ね描きのテストを追加（失敗するテスト）**

`test/radar.test.mjs` の末尾（最後の閉じ括弧の前）に追記：
```javascript
test("buildRadarOverlaySVG は2つのデータポリゴンを描く", () => {
  const items = ["年収","勤務地","労働時間","在宅","裁量","安定"];
  const A = items.map((l,i)=>({label:l, value:i/5}));
  const B = items.map((l,i)=>({label:l, value:1-i/5}));
  const svg = R.buildRadarOverlaySVG(A, B);
  assert.match(svg, /class="data-poly-a"/);
  assert.match(svg, /class="data-poly-b"/);
  assert.match(svg, /<svg/);
});
```
（`R` は同ファイル冒頭の `import R from "../radar.js"` を利用。未定義なら冒頭に追加。）

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
( cd value-compass/frontend && node --test test/radar.test.mjs )
```
期待：FAIL（`buildRadarOverlaySVG` 未定義）。

- [ ] **Step 3: radar.js に buildRadarOverlaySVG を実装**

`radar.js` の `buildRadarSVG` 関数の直後（`return s+\`</svg>\`;}` の後、`return { buildRadarSVG };` の前）に追加：
```javascript
  function buildRadarOverlaySVG(itemsA, itemsB){
    if(!itemsA || itemsA.length<3) return '<svg viewBox="0 0 320 300" role="img" aria-label="重視度レーダー"></svg>';
    const n=itemsA.length;
    const ang=itemsA.map((_,i)=> (-90 + i*(360/n)) * Math.PI/180);
    const pt=(r,i)=>[ +(CX+r*Math.cos(ang[i])).toFixed(1), +(CY+r*Math.sin(ang[i])).toFixed(1) ];
    let s=`<svg viewBox="0 0 320 300" width="300" height="282" role="img" aria-label="2シナリオ重視度レーダー">`;
    [0.25,0.5,0.75,1].forEach(ring=>{
      const p=itemsA.map((_,i)=>pt(R*ring,i).join(",")).join(" ");
      s+=`<polygon class="grid-line" points="${p}"/>`;
    });
    itemsA.forEach((_,i)=>{ const [x,y]=pt(R,i); s+=`<line class="axis-line" x1="${CX}" y1="${CY}" x2="${x}" y2="${y}"/>`; });
    const poly=(items,cls)=>{
      const dp=items.map((d,i)=>pt(R*Math.max(0,Math.min(1,d.value)),i).join(",")).join(" ");
      return `<polygon class="${cls}" points="${dp}"/>`;
    };
    s+=poly(itemsA,"data-poly-a")+poly(itemsB,"data-poly-b");
    itemsA.forEach((d,i)=>{ const [x,y]=pt(LR,i); const c=Math.cos(ang[i]), sn=Math.sin(ang[i]);
      const anchor=c>0.3?"start":(c<-0.3?"end":"middle"); const dy=sn<-0.3?-2:(sn>0.3?12:4);
      s+=`<text class="axis-label" x="${x}" y="${y+dy}" text-anchor="${anchor}">${esc(d.label)}</text>`; });
    return s+`</svg>`;
  }
```

export 行を更新：
```javascript
  return { buildRadarSVG, buildRadarOverlaySVG };
```

- [ ] **Step 4: テストを実行して PASS を確認**

```bash
( cd value-compass/frontend && node --test test/radar.test.mjs )
```
期待：PASS。

- [ ] **Step 5: コミット**

```bash
git add value-compass/frontend/radar.js value-compass/frontend/test/radar.test.mjs
git commit -m "feat(radar): 2シナリオ重ね描きSVG"
```

---

## Task 4: アプリ進行を2ブロック化（シーケンス生成）

`app.js` の `buildSequence` を、練習→シナリオ①（10問＋支配）→切替→シナリオ②（10問）に変更し、シナリオ提示順を回答者間でランダム化する。回答に `scenario` を付与する。純粋なシーケンス組み立ては単体テスト可能な関数として切り出す。

**Files:**
- Modify: `value-compass/frontend/app.js`
- Test: `value-compass/frontend/test/sequence.test.mjs`（新規）

- [ ] **Step 1: シーケンス組み立てを純関数として切り出す設計でテストを書く（失敗するテスト）**

`test/sequence.test.mjs` を新規作成：
```javascript
import { test } from "node:test";
import assert from "node:assert";
import { readFileSync } from "node:fs";
import S from "../sequence.js";

const data = JSON.parse(readFileSync(new URL("../questions.json", import.meta.url)));

test("シーケンスは 練習1 + 本番20 + 支配1 = 22 要素", () => {
  const seq = S.buildSequence(data.questions, () => 0.5);
  assert.equal(seq.length, 22);
  assert.equal(seq.filter(q => q.type === "practice").length, 1);
  assert.equal(seq.filter(q => q.type === "main").length, 20);
  assert.equal(seq.filter(q => q.type === "dominant").length, 1);
});

test("各本番設問に scenario が付き、同一シナリオは連続ブロックになる", () => {
  const seq = S.buildSequence(data.questions, () => 0.5);
  const mains = seq.filter(q => q.type === "main");
  const firstScenario = mains[0].scenario;
  // 先頭ブロック10問が同一シナリオ、後半10問がもう一方
  for (let i = 0; i < 10; i++) assert.equal(mains[i].scenario, firstScenario);
  for (let i = 10; i < 20; i++) assert.notEqual(mains[i].scenario, firstScenario);
});

test("提示順はrandで反転する（A先/B先）", () => {
  const seqLow = S.buildSequence(data.questions, () => 0.1);
  const seqHigh = S.buildSequence(data.questions, () => 0.9);
  const first = s => s.filter(q => q.type === "main")[0].scenario;
  assert.notEqual(first(seqLow), first(seqHigh));
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
( cd value-compass/frontend && node --test test/sequence.test.mjs )
```
期待：FAIL（`../sequence.js` が無い）。

- [ ] **Step 3: sequence.js を新規作成（UMD・純関数）**

`value-compass/frontend/sequence.js` を新規作成：
```javascript
/* sequence.js — 設問シーケンス組み立て（純関数・テスト可能） */
(function (root, factory){
  const api=factory();
  if (typeof module!=="undefined" && module.exports) module.exports=api;
  if (typeof window!=="undefined") window.ValueCompassSequence=api;
})(this, function(){
  "use strict";
  function shuffle(arr, rnd){
    const a=arr.slice();
    for(let i=a.length-1;i>0;i--){ const j=Math.floor(rnd()*(i+1)); [a[i],a[j]]=[a[j],a[i]]; }
    return a;
  }
  // rnd: ()=>[0,1) の乱数。テスト用に注入可能。
  function buildSequence(questions, rnd){
    rnd = rnd || Math.random;
    const practice = questions.find(q=>q.type==="practice");
    const dominant = questions.find(q=>q.type==="dominant");
    const A = shuffle(questions.filter(q=>q.type==="main" && q.scenario==="A"), rnd);
    const B = shuffle(questions.filter(q=>q.type==="main" && q.scenario==="B"), rnd);
    const bFirst = rnd() < 0.5;
    const [first, second] = bFirst ? [B, A] : [A, B];
    // 支配選択は先頭ブロックの中盤（4〜6番目）に挿入
    const block1 = first.slice();
    const pos = 4 + Math.floor(rnd()*3);
    block1.splice(pos, 0, dominant);
    return [practice, ...block1, ...second];
  }
  return { buildSequence, shuffle };
});
```

- [ ] **Step 4: テストを実行して PASS を確認**

```bash
( cd value-compass/frontend && node --test test/sequence.test.mjs )
```
期待：PASS（3テスト）。

- [ ] **Step 5: app.js から sequence.js を使うよう改修**

`app.js:60-72`（`shuffle` と `buildSequence`）を削除し、`init` 内の `SEQUENCE = buildSequence(json.questions);`（`app.js:51`）を：
```javascript
        SEQUENCE = window.ValueCompassSequence.buildSequence(json.questions);
```
に変更。`onChoose`（`app.js:142`）で回答に scenario を付与：
```javascript
    answers.push({ q_id: q.id, choice: choice, response_ms: ms, scenario: q.scenario || null });
```
`index.html` の `<script>` 読み込みに `sequence.js` を `app.js` より前に追加（Task 6 で実施）。

- [ ] **Step 6: コミット**

```bash
git add value-compass/frontend/sequence.js value-compass/frontend/app.js value-compass/frontend/test/sequence.test.mjs
git commit -m "feat(app): 2ブロック・シナリオ順ランダム化のシーケンス（純関数化）"
```

---

## Task 5: 結果リビールに6カード目「2つの人生で変わるあなた」を追加

`app.js` の `buildReveal` を `estimate2` ベースに更新し、シナリオAレーダー＋6カード目（重ねレーダー＋ΔWTP＋動かない核）を表示。`computeAndShowResult` を `estimate2` 呼び出しに変更。

**Files:**
- Modify: `value-compass/frontend/app.js`

- [ ] **Step 1: computeAndShowResult を estimate2 ベースに変更**

`app.js:193-202` を置換：
```javascript
  function computeAndShowResult() {
    $("#progress-bar-fill").style.width = "100%";
    const E = window.ValueCompassEstimate;
    const est2 = E.estimate2(DATA.questions, META, answers);
    const ex = E.extras(DATA.questions, META, answers, { beta: est2.beta_A, importance: est2.importance_A, importance_rank: est2.importance_rank_A, mrs_manyen: est2.mrs_A_manyen });
    estResult = { est2: est2, extras: ex };
    buildReveal(est2, ex);
    if (!ex.quality.dominant_passed) $("#quality-note").style.display = "block";
    show("result");
  }
```

- [ ] **Step 2: buildReveal を est2 対応＋6カード目追加に改修**

`app.js:206` の `function buildReveal(est, ex){` の引数を `est2` に読み替える。`app.js:209-210` の radar 構築を：
```javascript
    const radarLabels = { income:"年収", location:"勤務地", hours:"労働時間", remote:"在宅勤務", growth:"裁量・成長", stability:"安定性" };
    const itemsA = order.map(a=>({ label: radarLabels[a], value: est2.importance_A[a] }));
    const itemsB = order.map(a=>({ label: radarLabels[a], value: est2.importance_B[a] }));
    const radarSVG = window.ValueCompassRadar.buildRadarSVG(itemsA);
    const overlaySVG = window.ValueCompassRadar.buildRadarOverlaySVG(itemsA, itemsB);
```
`app.js:212` 以降の `est.mrs_manyen`/`est.importance_rank` 参照を `est2.mrs_A_manyen`/`est2.importance_rank_A` に置換（カード1〜4は v0.2 同様、シナリオA基準で表示）。

カード配列（`app.js:236-247`）の各 `kicker` を `/ 6` に変更し、末尾（カード5の後）に6枚目を追加：
```javascript
      { kicker:"DISCOVERY 6 / 6", title:"2つの人生で変わるあなた",
        html:(function(){
          const lab = ex.verbal.label;
          // 最も動いた属性（|delta|最大、nullは除外）
          const movers = order.filter(a=>a!=="income" && est2.delta_wtp_manyen[a]!==null)
            .sort((x,z)=>Math.abs(est2.delta_wtp_manyen[z])-Math.abs(est2.delta_wtp_manyen[x]));
          const top = movers[0];
          const moverText = top
            ? `子育て中のあなたは、<strong>${lab[top]}</strong>の価値が ${est2.delta_wtp_manyen[top]>=0?"+":""}${est2.delta_wtp_manyen[top]}万円ぶん ${est2.delta_wtp_manyen[top]>=0?"上がりました":"下がりました"}。`
            : `2つの人生で、価値の重みづけに大きな差は出ませんでした。`;
          // 動かない核（|delta|最小の上位2、null除外）
          const core = movers.slice().sort((x,z)=>Math.abs(est2.delta_wtp_manyen[x])-Math.abs(est2.delta_wtp_manyen[z])).slice(0,2);
          const coreText = core.length ? core.map(a=>lab[a]).join("・") : "—";
          return `<p class="lead">独身のあなた（青）と、子育て中のあなた（赤）の重視度を重ねました。</p>`
            + `<div class="radar-wrap">${overlaySVG}</div>`
            + `<p class="money-sub">${moverText}</p>`
            + `<div class="box keep" style="margin-top:12px"><h4>どんな人生でも譲れない核</h4><p>${coreText}</p></div>`;
        })() }
```

- [ ] **Step 3: ローカルで手動確認（簡易サーバ）**

```bash
( cd value-compass/frontend && python3 -m http.server 8765 )
```
ブラウザで `http://localhost:8765/` を開き、練習→ブロック①→切替→ブロック②→結果まで通し、6枚目に重ねレーダー・ΔWTP文・核が出ることを確認。
（切替画面・シナリオ提示は Task 6 で実装するため、この時点では Task 6 と合わせて最終確認する。）

- [ ] **Step 4: コミット**

```bash
git add value-compass/frontend/app.js
git commit -m "feat(result): 6カード目（2シナリオ重ねレーダー・ΔWTP・動かない核）"
```

---

## Task 6: シナリオ提示/切替画面・スタイル・スクリプト読み込み

`index.html` にシナリオ提示画面と切替画面を追加し、`app.js` の進行に組み込む。`style.css` に重ねレーダー2色とシナリオ画面のスタイルを追加。`sequence.js` を読み込む。

**Files:**
- Modify: `value-compass/frontend/index.html`
- Modify: `value-compass/frontend/app.js`
- Modify: `value-compass/frontend/style.css`

- [ ] **Step 1: index.html にシナリオ提示・切替の screen を追加**

`index.html` の `#question` screen の直前に、2つの screen を追加（クラス名・id は既存 `.screen`/`show()` 規約に合わせる）：
```html
<section id="scenario-intro" class="screen">
  <div class="wrap">
    <p class="kicker" id="scn-kicker"></p>
    <h2 id="scn-title"></h2>
    <p class="lead" id="scn-desc"></p>
    <button id="scn-begin-btn" class="primary">この人生で答える →</button>
  </div>
</section>
<section id="scenario-switch" class="screen">
  <div class="wrap">
    <p class="kicker">ここからは別の人生のあなたで</p>
    <h2 id="switch-title"></h2>
    <p class="lead" id="switch-desc"></p>
    <button id="switch-begin-btn" class="primary">次の人生で答える →</button>
  </div>
</section>
```
さらに `<head>` 末尾付近のスクリプト読み込みで、`app.js` の前に `sequence.js` を追加：
```html
<script src="sequence.js"></script>
```
（既存の `radar.js`・`estimate.js`・`app.js` の並びに合わせ、`app.js` より前に置く。）

- [ ] **Step 2: app.js にシナリオ画面の制御を追加**

`META.framing.scenarios` の定義文をシナリオ画面に出すための定数を `app.js` 冒頭の状態付近（`app.js:20` 付近）に追加：
```javascript
  const SCN = {
    A: { key:"single_no_plan",   title:"独身・結婚の予定なし", desc:"30歳のあなた。独身で、当面結婚の予定はありません。自分の暮らしを基準に、これから就く仕事を選んでください。正解はありません。率直に。" },
    B: { key:"married_one_child", title:"既婚・子ども1人（育児中）", desc:"30歳のあなた。結婚していて、小さな子どもが1人います。家庭との両立も考えながら、これから就く仕事を選んでください。正解はありません。率直に。" }
  };
```
`onChoose` の `advance`（`app.js:155-160`）を、ブロック境界でシナリオ画面を挟むよう変更：
```javascript
    const advance = function () {
      if (cursor >= SEQUENCE.length) { computeAndShowResult(); return; }
      if (q.type === "practice") { show("transition"); return; }
      const prev = SEQUENCE[cursor-1], next = SEQUENCE[cursor];
      // 本番→本番でシナリオが切り替わる境界なら切替画面
      if (prev && next && prev.type==="main" && next.type==="main" && prev.scenario!==next.scenario) {
        showScenarioSwitch(next.scenario); return;
      }
      renderQuestion();
    };
```
`bindTransition`（`app.js:84-89`）の「練習→本番」案内ボタンで、最初のシナリオ提示画面を出すよう変更：
```javascript
  function bindTransition() {
    $("#begin-main-btn").addEventListener("click", function () {
      const firstScn = SEQUENCE.find(q=>q.type==="main").scenario;
      showScenarioIntro(firstScn);
    });
    $("#scn-begin-btn").addEventListener("click", function () { renderQuestion(); show("question"); });
    $("#switch-begin-btn").addEventListener("click", function () { renderQuestion(); show("question"); });
  }
  function showScenarioIntro(scn){
    $("#scn-kicker").textContent = "1つ目の人生";
    $("#scn-title").textContent = SCN[scn].title;
    $("#scn-desc").textContent = SCN[scn].desc;
    show("scenario-intro");
  }
  function showScenarioSwitch(scn){
    $("#switch-title").textContent = SCN[scn].title;
    $("#switch-desc").textContent = SCN[scn].desc;
    show("scenario-switch");
  }
```

- [ ] **Step 3: style.css に重ねレーダー2色とシナリオ画面スタイルを追加**

`style.css` 末尾に追記：
```css
/* v0.3: 2シナリオ重ねレーダー */
.data-poly-a { fill: rgba(63,111,216,.22); stroke:#3f6fd8; stroke-width:2; }
.data-poly-b { fill: rgba(210,96,74,.22);  stroke:#d2604a; stroke-width:2; }
/* v0.3: シナリオ提示・切替画面 */
#scenario-intro .lead, #scenario-switch .lead { max-width:32em; }
#scn-kicker { letter-spacing:.08em; color:#5b6679; font-weight:700; font-size:13px; }
```

- [ ] **Step 4: 通し動作の手動確認**

```bash
( cd value-compass/frontend && python3 -m http.server 8765 )
```
`http://localhost:8765/` で全フロー（導入→練習→案内→シナリオ①提示→10問→切替→シナリオ②提示→10問→結果6枚→確認）を確認。シナリオ提示順がリロードで入れ替わること、結果6枚目に重ねレーダーが2色で出ることを確認。

- [ ] **Step 5: 全テストを実行**

```bash
( cd value-compass/frontend && node --test )
```
期待：全 PASS。

- [ ] **Step 6: コミット**

```bash
git add value-compass/frontend/index.html value-compass/frontend/app.js value-compass/frontend/style.css
git commit -m "feat(ui): シナリオ提示/切替画面・重ねレーダー配色・sequence.js読込"
```

---

## Task 7: 送信payload・Worker・CSVエクスポートを v0.3 対応

`app.js` の `buildPayload` を v0.3 構造に、Worker の保存フィールドを拡張、`export-from-kv.mjs` のCSV列に v0.3 推定値を追加。

**Files:**
- Modify: `value-compass/frontend/app.js`
- Modify: `value-compass/worker/src/index.js`
- Modify: `export-from-kv.mjs`

- [ ] **Step 1: buildPayload を v0.3 構造に変更**

`app.js:277-292` を置換：
```javascript
  function buildPayload(agreement, freeText) {
    const est2 = estResult.est2, ex = estResult.extras;
    const blocks = { A:{ scenario:"single_no_plan", answers:[] }, B:{ scenario:"married_one_child", answers:[] } };
    answers.forEach(a => { if (a.scenario === "A") blocks.A.answers.push(a); else if (a.scenario === "B") blocks.B.answers.push(a); });
    const firstScn = SEQUENCE.find(q=>q.type==="main").scenario;
    return {
      session_id: sessionId,
      timestamp: new Date().toISOString(),
      framing: META.framing,
      scenario_order: firstScn==="A" ? ["single_no_plan","married_one_child"] : ["married_one_child","single_no_plan"],
      question_order: SEQUENCE.map(q=>q.id),
      blocks: blocks,
      answers: answers,
      estimate: {
        method: "pooled_binary_logit_ridge_interaction",
        beta_A: est2.beta_A, gamma: est2.gamma,
        mrs_A_manyen: est2.mrs_A_manyen, mrs_B_manyen: est2.mrs_B_manyen,
        delta_wtp_manyen: est2.delta_wtp_manyen,
        importance_rank_A: est2.importance_rank_A, importance_rank_B: est2.importance_rank_B,
        scale_diff: est2.scale_diff, fallback_constrained: est2.fallback_constrained, converged: est2.converged
      },
      counts: ex.counts,
      decisive: ex.decisive,
      quality: Object.assign({}, ex.quality, { scale_diff: est2.scale_diff, fallback_constrained: est2.fallback_constrained }),
      feedback: { agreement: agreement, free_text: freeText },
      app_version: "0.3"
    };
  }
```

- [ ] **Step 2: Worker の保存レコードに v0.3 フィールドを追加**

`value-compass/worker/src/index.js` の `record` オブジェクト（`index.js` 内 `const record = {...}`）に項目を追加：
```javascript
    blocks: payload.blocks || null,
    scenario_order: payload.scenario_order || null,
```
（既存の `estimate`・`quality`・`decisive` 等はそのまま保持される。受信バリデーションは `answers` 配列必須のまま変更不要。）

- [ ] **Step 3: export-from-kv.mjs のCSV列に v0.3 推定値を追加**

`export-from-kv.mjs` のヘッダ配列と行マッピングに、次の列を追加（既存の列定義に倣って追記）：
```
scenario_order,
beta_A_income, beta_A_location, beta_A_hours, beta_A_remote, beta_A_growth, beta_A_stability,
gamma_location, gamma_hours, gamma_remote, gamma_stability,
mrsA_location, mrsA_hours, mrsA_remote, mrsA_growth, mrsA_stability,
mrsB_location, mrsB_hours, mrsB_remote, mrsB_growth, mrsB_stability,
delta_hours, delta_remote, delta_location, delta_stability,
scale_diff, fallback_constrained
```
値は `record.estimate.beta_A.*` / `record.estimate.gamma.*` / `record.estimate.mrs_A_manyen.*` / `record.estimate.mrs_B_manyen.*` / `record.estimate.delta_wtp_manyen.*` / `record.estimate.scale_diff` / `record.estimate.fallback_constrained` から取得。`null`/未定義は空文字にフォールバック。`scenario_order` は `record.scenario_order.join("|")`。
（実ファイルの既存ヘルパ関数・列順に合わせて差し込む。CSVのUTF-8 BOM付与など既存処理は踏襲。）

- [ ] **Step 4: Worker をローカル起動して送信を検証**

```bash
( cd value-compass/worker && wrangler dev ) &
# 別シェルで:
curl -s -X POST http://127.0.0.1:8787/api/submit \
  -H "Content-Type: application/json" \
  -d '{"session_id":"test-v03","answers":[{"q_id":"a01","choice":"A","scenario":"A"}],"app_version":"0.3","scenario_order":["single_no_plan","married_one_child"],"estimate":{"beta_A":{"income":0.01}}}'
```
期待：`{"ok":true,"session_id":"test-v03"}`。確認後 `wrangler dev` を停止。

- [ ] **Step 5: コミット**

```bash
git add value-compass/frontend/app.js value-compass/worker/src/index.js export-from-kv.mjs
git commit -m "feat(payload/worker/export): v0.3フィールド（blocks・gamma・mrsA/B・delta・scale）"
```

---

## Task 8: デプロイとスモークテスト

新インフラに本番デプロイし、エンドツーエンドで動作確認する。

**Files:**
- なし（デプロイ操作）

- [ ] **Step 1: Worker をデプロイし EXPORT_TOKEN を設定**

```bash
cd /Users/munetomoando/claude-work/nuce/compass-scenario
bash deploy-worker.sh
( cd value-compass/worker && wrangler secret put EXPORT_TOKEN )
```
期待：`value-compass-scenario.ando-munetomo.workers.dev` にデプロイ完了。

- [ ] **Step 2: フロントを gh-pages に公開**

```bash
bash deploy-frontend.sh
bash enable-pages.sh
```
期待：`munetomoando.github.io/value-compass-scenario/` が公開（反映に数分）。

- [ ] **Step 3: 本番URLでスモークテスト**

ブラウザで `https://munetomoando.github.io/value-compass-scenario/` を開き、最後まで回答→送信が `回答を受け付けました。` になることを確認。

- [ ] **Step 4: KVにレコードが入ったか確認**

```bash
cd /Users/munetomoando/claude-work/nuce/compass-scenario
node export-from-kv.mjs
```
期待：v0.3列を含むCSVが出力され、スモークテストのセッションが含まれる。

- [ ] **Step 5: v0.2 への無影響を確認**

`https://munetomoando.github.io/value-compass/`（v0.2）が従来どおり動作し、v0.2 のKVに新レコードが混ざっていないことを確認（別Worker・別KVのため理論上不可だが目視確認）。

- [ ] **Step 6: 最終コミット・push**

```bash
git add -A && git commit -m "chore: v0.3 deployment ready" --allow-empty
git push origin main
```

---

## 完了条件

- [ ] `( cd value-compass/frontend && node --test )` が全 PASS
- [ ] 本番 `value-compass-scenario` URL で 22画面フローが通り、結果6枚目に2シナリオ重ねレーダーとΔWTPが表示される
- [ ] 送信レコードが新KVに保存され、`export-from-kv.mjs` が v0.3 列付きCSVを出力する
- [ ] v0.2（学生展開中）に一切変更・影響がない
