# 設計書：価値観コンパス v0.2 ―「めくって発見」体験＆ロジット推定への拡張

**親設計**: `design_dce_career_app.md`（MVP v0.1）
**本書の位置づけ**: v0.1 の機能検証MVPを土台に、(a) 体験の面白さ と (b) 終了後の自己理解の深さ を高めるための拡張設計。
**対象**: 大学生（授業教材・機能検証）
**制約**: Cloudflare Workers ＋ GitHub Pages の無料枠。推定はクライアント側で完結。
**バージョン**: v0.2（discovery experience）
**作成**: 安藤至大 (Munetomo Ando)

---

## 1. 背景と狙い

v0.1 は DCE（離散選択実験）として選好を引き出す手法的有効性はあるが、回答体験が淡々として面白みに欠け、結果も受動的な「読み物」だった。本拡張は次の2点を同時に高める。

- **面白さ＝発見・洞察型**：回答するほど「自分はこう考えるのか」という気づきが積み上がる体験。
- **自己理解の深さ**：終了後に、お金換算（限界代替率）と「決定的だった瞬間」を通じて、自分の選好を具体的に理解できる。

測定の正確さと面白さはトレードオフになりうるため、**進行中は解釈を出さず（事実のみ）、解釈は最後にまとめて**届ける方針で両立させる。

---

## 2. 確定した設計判断（ブレストの結論）

| 論点 | 決定 |
|------|------|
| 面白さの方向 | **発見・洞察型**（進行中に気づきが積み上がる） |
| 洞察のタイミング | **進行中は事実のトレードオフのみ／解釈は最後** |
| 結果の中身 | **お金換算の価値（MRS）** ＋ **決定的だった瞬間**（応答時間・接戦度） |
| 推定エンジン | **個人レベルの条件付き（=差分の二項）ロジット** ＋ 軽い正則化。MRSを金額換算 |
| 体験の組み立て | **B案：めくって発見**（結果を1枚ずつ段階的に開く） |
| 重視度の可視化 | **レーダーチャート（六角形）** |
| income の意味 | **「その仕事に就いた30歳頃の額面年収」** と明記 |
| income の水準 | **4水準 300 / 450 / 600 / 750（万円、等間隔150）**。線形コーディング、MRSは万円表示 |
| 家族・ライフステージ前提 | **固定せず「今のあなたの率直な感覚で」**。時点は「30歳頃のあなた」に統一して明記 |
| 設問数 | **練習1＋本番16＋注意チェック1＝計18画面**（有効16問）。提示順ランダム化・進捗分割表示 |

---

## 3. 体験フロー（改訂）

```
[1] 導入・同意        既存＋意味付け/前提の明記（§4.3）。「手応えのある発見体験」と予告
[2] 練習1問 → 案内画面  既存（注意チェックは本番に紛れ込ませ済み）
[3] 本番設問 ×16+注意1  ★拡張。提示順ランダム化、進捗分割表示
     └ 選択直後：ページめくり前に「いま、あなたは〔在宅勤務〕を〔年収〕と
        引き換えに選びました」という"事実のトレードオフ"だけを淡く約0.6秒提示（§7）
[4] 推定処理          ★クライアント側ロジット（§6）
[5] 結果＝めくって発見  ★5枚のカードを1枚ずつ開く（§8）
[6] 確認画面          既存（納得度5段階＋自由記述）
[7] 送信完了          既存（Worker未設定ならローカル完結）
```

変更は [3][4][5]。[1][2][6][7] は v0.1 を流用（[1] は文言追記のみ）。

---

## 4. 属性・水準の改訂

### 4.1 属性一覧（6属性は維持）

| ID | 属性 | 水準 | 符号化 |
|----|------|------|--------|
| `income` | 年収（30歳頃・額面） | 300 / 450 / 600 / 750（万円） | **連続（万円）**。β_income=1万円あたり効用 |
| `location` | 勤務地・転勤 | 転勤なし / あり | 二値（なし=良い水準=1） |
| `hours` | 労働時間 | 残業少 / 多 | 二値（少=1） |
| `remote` | 在宅勤務 | 在宅可 / 出社のみ | 二値（可=1） |
| `growth` | 成長・裁量 | 裁量大 / 小 | 二値（裁量大=1、方向はβの符号で判定） |
| `stability` | 雇用安定性 | 安定 / 不安定 | 二値（安定=1、方向はβの符号で判定） |

### 4.2 income を4水準にした理由

- 基準財（numéraire）の変動を増やし **β_income の識別を安定化** → MRS（お金換算）の精度向上。
- 等間隔150万にすることで **線形近似が生きる**（300→750は2.5倍に収め、限界効用逓減の非線形バイアスと「年収支配（attribute non-attendance）」を回避）。1000万を入れると年収支配・非現実回答・非線形化のリスクが大きいため採らない。

### 4.3 時点・前提の明記（指示文）

導入画面と各設問の補足に、次の主旨を1文で表示する。

> これから就く仕事として選んでください。年収はその仕事に就いた**30歳頃の額面**、転勤・在宅などは**その仕事がずっと持つ特徴**です。特定の家族構成は思い描かず、**"今のあなた"の率直な感覚**で選んでください。

これにより (1) 属性間の想定時点を「30歳頃のあなた」に統一し、(2) 家族形態という隠れた異質性を指示で標準化する。

---

## 5. 設問設計（16問への拡張）

- **目的**：個人レベルのロジット推定の安定化（親設計§5.2＝v1段階の16〜20問に対応）。20問は後半の回答品質低下のため避け、**16問**を採用。
- **手作りの直交配列に近い固定セット**を設計者が用意し、各属性が A/B で異なる回数（分岐回数）を **6属性すべてで十分（目安8〜12問）** 確保。`income` は4水準が偏らないよう配分。
- **提示順ランダム化**：本番16問はセッションごとにシャッフル（順序効果・属性無視の抑制）。練習は先頭固定、注意チェック（支配選択）は中盤付近にランダム挿入。
- **検証スクリプト**：`questions.json` 生成時に分岐回数・income水準分布・支配選択の妥当性を自動チェック（v0.1 で確立した検証を16問に拡張）。
- **識別性の確認**：6パラメータ（income連続1＋二値5）。差分デザイン行列のランクが満たされること、income列に十分な分散があることをビルド時に確認。

---

## 6. 推定エンジン（クライアント側ロジット）

### 6.1 モデル
2択強制選択を **差分の二項ロジット**（=2択の条件付きロジット）で推定。
- 各設問の差分ベクトル d = x_A − x_B。
- P(Aを選ぶ) = logistic(α + β·d)。α は左右位置バイアス吸収用の微小切片（報告はβのみ）。
- パラメータ：β_income（万円あたり）, β_location, β_hours, β_remote, β_growth, β_stability。

### 6.2 推定法
- **罰則付き最尤（リッジ, 小さなλ）** を Newton–Raphson（IRLS）で。6〜7パラメータ×16観測で起こりやすい**完全分離**（一貫回答者で β→∞）を有限に安定化。
- 収束しない/異常時はλを段階的に強める安全装置。反復上限と発散検知を実装。

### 6.3 お金換算（限界代替率）
- 価値[万円] = β_attr / β_income。例：β_remote/β_income=90 →「在宅勤務は年収90万円ぶんの価値」。
- **エッジケース**：β_income がほぼ0／符号が逆（年収をほぼ重視せず）→ 金額が発散・無意味化するため**換算を出さず**「年収をあまり重視しなかったため換算を省略」と表示。極端値は上限でクリップし「目安」と明記。

### 6.4 決定的だった瞬間
- 対象は**採点対象の本番設問のみ**（練習・注意チェック=支配選択は除外。支配選択は構造上必ず即決になり気づきにならないため）。
- 各設問で「本人の効用での僅差度 = |β·d|」と応答時間 `response_ms` を併用。
- **いちばん悩んだ**：僅差（|β·d|小）かつ時間長。**即決**：大差かつ短時間。該当設問のトレードオフ内容を文章で添える。

### 6.5 カウントベースとの整合チェック（裏方）
- v0.1 のカウントベース順位とロジット順位を比較。大きく食い違えば品質フラグ（親設計§6.2「整合を検証してから載せ替える」に対応）。表には出さずログに残す。

---

## 7. 進行中の「事実トレードオフ」提示

- 選択直後、ページめくり演出の前に約0.6秒、淡色で1行表示：
  「いま、あなたは〔より良かった条件X〕を〔犠牲にした条件Y〕と引き換えに選びました」。
- **解釈・傾向・スコアは一切出さない**（反応性による測定バイアスを避ける）。XYは当該設問でA/Bが対立した属性から、選んだ側で良くなった属性と悪くなった属性を1つずつ抽出。
- 支配選択（注意チェック）と練習では出さない、または無難な表現にする。

---

## 8. 結果「めくって発見」（B案）

5枚のカードを「次の発見へ」で1枚ずつ開く（ページめくりアニメ＋進捗ドット）。

1. **重視度コンパス**：6属性の**レーダーチャート（六角形）**。中心から外へ張り出すほど重視。中心から展開するアニメ。形が価値観の"指紋"になる。
2. **お金に換算すると**：上位属性のMRSをカード表示（「残業少＝+120万円ぶん」等）。§6.3のエッジケース処理込み。
3. **決定的だった瞬間**：いちばん悩んだ選択／即決した選択を、応答時間つきで提示（§6.4）。
4. **譲れない線・出せる線**：上位2＝譲りにくい、下位2＝交換に出しやすい。subjective属性は方向ラベル付き。
5. **DCE種明かし**：手法の説明（任意表示）。

各カードはレーダー／金額／設問内容を推定結果から動的生成。視覚仕様はモック `result-reveal-v2.html` に準拠。

---

## 9. データモデル更新（§9.1 v0.1 を拡張）

```json
{
  "session_id": "uuid",
  "timestamp": "ISO8601",
  "framing": { "income_anchor": "age30_gross", "family_assumption": "none_current_self" },
  "question_order": ["q07","q03", "..."],
  "answers": [ { "q_id": "q03", "choice": "A", "response_ms": 4200 } ],
  "estimate": {
    "method": "binary_logit_ridge",
    "lambda": 0.5,
    "converged": true,
    "beta": { "income": 0.012, "location": 0.7, "hours": 1.4, "remote": 1.1, "growth": 0.3, "stability": -0.1 },
    "mrs_manyen": { "location": 55, "hours": 120, "remote": 90, "growth": 25, "stability": null },
    "importance_rank": ["hours","remote","location","growth","income","stability"]
  },
  "counts": { "income": 0.6, "location": 0.4, "hours": 0.8, "remote": 0.7, "growth": 0.5, "stability": 0.3 },
  "decisive": { "most_hesitated_qid": "q09", "fastest_qid": "q11" },
  "quality": { "dominant_passed": true, "min_response_ms": 1800, "logit_count_divergence": 0.12 },
  "feedback": { "agreement": 4, "free_text": "..." },
  "app_version": "0.2"
}
```

- 匿名方針（個人識別情報を取得しない）は v0.1 を踏襲。
- Worker（`/api/submit`・`/api/export`）は受け取るJSONが拡張されるため、CSV出力列に beta・mrs・decisive を追加。

---

## 10. ファイル構成・変更点

```
value-compass/frontend/
├── index.html      ［改修］導入文言、進行中トレードオフ表示、結果5カードのDOM
├── app.js          ［改修］16問・順序ランダム化、進行中トレードオフ、結果リビール制御
├── questions.json  ［刷新］16問＋意味付けメタ、income4水準、検証メタ
├── estimate.js     ［刷新］ロジット推定（IRLS＋リッジ）、MRS、決定的瞬間、カウント整合
├── radar.js        ［新規］レーダーチャート描画（SVG）  ※必要なら estimate.js とは分離
└── style.css       ［改修］レーダー・お金換算カード・決定的瞬間・リビール演出
value-compass/worker/
└── src/index.js    ［小改修］CSV出力列の追加（beta/mrs/decisive）
```

- 推定ロジック（数値計算）と描画（DOM/SVG）は別ファイルに保ち、単体で理解・テストできるようにする。
- ロジット推定は純関数 `estimate(questions, meta, answers) → {beta, mrs, rank, decisive, quality}` として切り出し、Node からテスト可能にする（v0.1 同様）。

---

## 11. 検証ゴールとの対応

| 親設計のゴール | 本拡張での効き方 |
|----------------|------------------|
| ①選好が引き出せるか | 進行中の事実トレードオフ提示で関与を高めつつ、解釈を出さず測定を保護 |
| ②結果が納得されるか | お金換算・レーダー・決定的瞬間で具体性が増し face validity を確認しやすい |
| ③気づきが生まれるか | 「めくって発見」と決定的瞬間が「自分はこれを重視していた」の気づきを誘発 |

---

## 12. 非スコープ／将来

- 混合ロジット（個人間異質性）、再受験の差分追跡、求人照合は将来（親設計§14）。
- 家族形態を冒頭で選ばせ「前提を変えると選好がどう変わるか」を見せる比較機能は将来拡張（今回は前提を置かない方針）。
- 内省プロンプト（C案）の組み込みは、結果カードへ後付けで追加可能な余地として残す。
- 授業データは商用転用しない方針を踏襲。

---

## 13. リスクと留意

- **少観測ロジットの不安定さ**：正則化と発散検知で対処。MRSは常に「目安」と明示。
- **年収支配**：income水準を等間隔150万に抑え、設問でincome差が常に最大化しないようバランス。
- **進行中表示による反応性**：事実のみ・解釈なしに限定。効果が疑わしければA/Bでオフ可能にする設計余地を残す。
- **認知負荷**：18画面で4〜7分。進捗分割表示と練習でならす。
